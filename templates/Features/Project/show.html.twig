{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-10">
            {{ project.name }}
        </div>
        <div class="col">
            <a href="{{ path('project_edit', {'id': project.id}) }}" class="btn btn-primary float-end">Edit</a>
        </div>
    </div>
{% endblock %}